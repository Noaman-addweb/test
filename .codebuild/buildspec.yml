version: 0.1
phases:
  pre_build:
     - echo $PRIVATE_KEY > ./codebuild/id_rsa
     - echo $HOSTS > ./codebuild/hosts
     - echo $KEY > ./codebuild/KEY
  build:
    commands:
      - cd .codebuild
      - ansible-playbook main.yml -vv 